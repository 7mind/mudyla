# properties

- `sequential`

# action: property-sequential

Simple action used to verify sequential default behavior.

```bash
set -euo pipefail

mkdir -p "test-output/properties"
echo "from-properties" > "test-output/properties/marker.txt"
```
